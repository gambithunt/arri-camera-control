# ARRICameraControlApp

This directory will hold the native iPad app target and SwiftUI shell.

Phase 1 creates the app shell structure while the core domain, protocol, discovery,
and session logic are built and tested inside Swift packages first.
